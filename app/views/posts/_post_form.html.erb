<%= form_for(@post) do |f| %>

    <%#= render 'users/error_messages', object: f.object %>

    <div class="field">

      <%= f.text_area :content,:class => "textarea_me", :placeholder => "Compose new post..." %>

    </div>

    <%= f.submit "Post", :class => "btn btn-primary" %>

<% end %>